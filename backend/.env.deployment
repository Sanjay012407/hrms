# Deployment Environment Configuration (Staging/Testing)
# Database Configuration (Using same as development for demo)
MONGODB_URI=mongodb+srv://hrms:7WA1i7BAB1XwQQRh@hrms.ppzwrf0.mongodb.net/?retryWrites=true&w=majority&appName=hrms-staging

# Server Configuration
PORT=5001
NODE_ENV=staging

# JWT Configuration
JWT_SECRET=hrms-staging-jwt-secret-2024-secure-256-bits
JWT_EXPIRES_IN=12h

# Email Configuration (SMTP)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=hrms-staging@yourcompany.com
EMAIL_PASS=CHANGE_TO_STAGING_APP_PASSWORD
EMAIL_FROM=HRMS Staging System <hrms-staging@yourcompany.com>

# CORS Configuration
CORS_ORIGIN=https://staging-hrms.yourcompany.com

# Session Configuration
SESSION_SECRET=hrms-staging-session-secret-2024-secure-key

# File Upload Configuration
MAX_FILE_SIZE=8388608
UPLOAD_PATH=/var/uploads/hrms-staging/

# Security Configuration
RATE_LIMIT_WINDOW_MS=600000
RATE_LIMIT_MAX_REQUESTS=200

# Logging Configuration
LOG_LEVEL=info
LOG_FILE_PATH=/var/log/hrms-staging/app.log

# SSL Configuration
SSL_CERT_PATH=/etc/ssl/certs/staging-hrms.crt
SSL_KEY_PATH=/etc/ssl/private/staging-hrms.key

# Database Pool Configuration
DB_MAX_POOL_SIZE=5
DB_MIN_POOL_SIZE=1

# Cache Configuration
REDIS_URL=redis://redis-staging:6379
CACHE_TTL=1800

# Testing Configuration
ENABLE_TEST_ROUTES=true
MOCK_EMAIL_SENDING=false
DEBUG_MODE=true

# Performance Monitoring
APM_SERVICE_NAME=hrms-staging
APM_ENVIRONMENT=staging
